---
title: 平台筛选与待标注快捷绑定决策地图
label: wayfinder:map
status: closed
---

## 终点

产出一份可直接交给开发 Agent 实施和验收的交互合同：精简平台概览并支持按扫描时卡槽筛选；待标注页以可搜索下拉按标签名称查找，在无匹配时快速新建，并把当前候选绑定到该标签且完成人工训练；短信正文通过独立详情页安全展示，并保留来源页面的返回与筛选上下文。

## 备注

- 这是新的独立 PRD；实现仍需遵守既有 [`MVP_SPEC.md`](../../MVP_SPEC.md) 的安全、隐私和分类合同，但不续用原 MVP 票据编号。
- 每次解决票据时使用 `grilling`；涉及交互取舍时使用 `prototype`。
- “卡槽”默认指短信进入索引时保存的历史卡槽快照；不得把当前卡槽、SIM 集合和历史卡槽混为一谈。
- 待标注仍一次只突出一条候选；标签绑定成功后沿用现有人工确认与端侧训练事务。
- 本地图默认只做规划。地图关闭后再用 `implement` 与 `tdd` 作为一个纵向切片实施。

## 已有决策

<!-- 关闭票据后在此添加一行索引；细节只保存在票据中。 -->

- [裁定平台概览的精简内容与卡槽筛选交互](0001-platform-overview-simplification.md) — 平台页按扫描时卡槽单选筛选，卡片改为筛选内聚合；详情证据沿用筛选并在返回后保留状态。
- [裁定待标注页的可搜索下拉与绑定语义](0002-pending-label-search-interaction.md) — 下拉首行搜索、次行固定快速创建；选择或新建只形成选择，必须显式“绑定并训练”，并保留可靠的提交与失败状态。
- [锁定标签查找去重与人工绑定的数据合同](0003-label-lookup-data-contract.md) — 本切片临时复用已分类平台为候选事实源，名称按统一规范化去重，快速创建仅在绑定训练成功时持久化，并要求按候选短信 id 原子幂等提交。
- [裁定独立短信详情页的导航与展示边界](0005-independent-message-detail.md) — 详情页按 `messageId` 实时读取 Provider，删除和撤权显示明确不可用状态，返回时恢复进入前的列表或平台证据上下文。
- [组装平台筛选与标签快捷绑定的实施验收合同](0004-platform-and-label-slice-contract.md) — 最小切片不新增标签实体或迁移，按 DAO/ViewModel/Compose/隐私/真机无线调试矩阵实施并验收。

## 尚未明确

- 无。实现阶段可在无线调试真机上调整 Compose 搜索下拉的具体尺寸与组件选型，但首行搜索、次行快速创建及可滚动行为不得改变。

## 不在范围

- SIM 集合持久化、按 SIM 集合筛选或把历史卡槽迁移为当前物理卡槽。
- 标签重命名、合并、别名、拆分的完整治理接通。
- 批量标注、多选待标注短信、待标注队列切换、下拉刷新或 LLM 批次标注。
- 拼音、模糊匹配库、远程标签搜索、平台详情分页和数据库无关重构。

## 子票据

本地图已关闭；实现阶段入口是 [`组装平台筛选与标签快捷绑定的实施验收合同`](0004-platform-and-label-slice-contract.md)。
